<nav class="navbar has-shadow category">
  <div class="container">
    <div class="navbar">
      <% @categories.each do |category|%>
        <%= link_to category.name,  root_path, class: "navbar-item" %>
      <% end %>
    </div>
  </div>
</nav>